# -----------------------------------------------------------------------------
# build.properties
# This file is referenced by the build.xml file.
#
# $Id$
# -----------------------------------------------------------------------------

# the version of this release
version=3.6

## comment the next property to deactivate it or uncomment it to activate it
meshcms.buildjar=buildjar  # build jar of meshcms or include as .class files

# javac target and source
javac.target=1.4
javac.source=1.4

# -----------------------------------------------------------------------------
# dirs and files
# -----------------------------------------------------------------------------
svndir  = meshcms3
build   = ${basedir}/build
dist    = ${basedir}/dist
build-essential = ${basedir}/build-essential
srcjava = ${basedir}/src/java
srctest = ${basedir}/src/test
srcweb  = ${basedir}/web
samples = ${basedir}/samples
readme  = ${basedir}/readme.txt
license = ${basedir}/license.txt
chglog  = ${basedir}/changelog.txt
todo    = ${basedir}/todo.txt
classes = ${build}/classes
jars    = ${build}/jars
javadoc = ${build}/javadoc
tagdoc  = ${build}/tagdoc
help    = ${srcweb}/meshcms/admin/help/en
test    = ${build}/test
webapp  = ${dist}/${ant.project.name}
warfile = ${dist}/${ant.project.name}.war
sampleswar = ${dist}/samples.war
# -----------------------------------------------------------------------------
# dev tools
# -----------------------------------------------------------------------------
tlddoc.dir     = ${build-essential}/taglibrarydoc-1_3
checkstyle.dir = ${build-essential}/checkstyle-4.1
checkstyle.jar = checkstyle-all-4.1.jar
junit.dir      = ${build-essential}/junit-3.8.1
junit.jar      = junit-3.8.1.jar
## -----------------------------------------------------------------------------
# svn
# -----------------------------------------------------------------------------
meshcms.svnroot.url=https://meshcms.svn.sourceforge.net/svnroot/meshcms

# -----------------------------------------------------------------------------
# all jar needed
# -----------------------------------------------------------------------------
servlet.jar=${build-essential}/libServlet2.3/servlet.jar

# -----------------------------------------------------------------------------
# external ant tasks
# -----------------------------------------------------------------------------
svnant.xml=${build-essential}/svnant.xml
